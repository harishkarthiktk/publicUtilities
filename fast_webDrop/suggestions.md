1) filter:
<select id="sort-select" class="sort-select">
                <option value="newest">Newest first</option>
                <option value="oldest">Oldest first</option>
                <option value="alpha">A-Z</option>
                <option value="domain">By Domain</option>
            </select>
must be within <div class="input-section"> at the top, and NOT in the nav bar.

2) the different download/export options are seperate.
- instead export and import should be a single drop down, and different types of export should be category within the export itself.
